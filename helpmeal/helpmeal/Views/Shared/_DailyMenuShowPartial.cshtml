@model DailyMealViewModel

    <form asp-controller="Meal" asp-action="@ViewData["Action"]" method="post">
        @foreach (var meal in Model.Meals)
        {
            <div class="form-group">
                <div>@meal.Recipe.Name</div>
                <label asp-for="@meal.Amount">Amount</label>
                <input type="text" asp-for="@meal.Amount" class="form-control" />
                <span asp-validation-for="@meal.Amount" class="text-danger"></span>
            </div>
        }
            
            
        <button type="submit" class="btn btn-primary">Edit</button>
    </form>
    <form>
        <button type="button" class="btn btn-info" onclick="history.back()">Cancel</button>
    </form>

    @*<form asp-controller="Meal" asp-action="Add" method="post">
        <div class="form-group">
            <label asp-for="RestaurantRequest.Name">@Localizer["Name of the restaurant"]</label>
            <input type="text" asp-for="RestaurantRequest.Name" class="form-control" />
            <span asp-validation-for="RestaurantRequest.Name" class="text-danger"></span>
        </div>
    </form>*@
